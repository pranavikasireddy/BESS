# notebooks/

Reserved for the forward-looking scenario work: the solar-cannibalization regression, the three penetration-level scenario runs, and the congestion comparison. Everything up to this point (data pipeline, heuristic, LP, imbalance overlay) is deliberately in `src/` as tested modules rather than notebooks, since it's reusable logic, not one-off exploration.
