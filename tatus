[1mdiff --git a/src/optimisation.py b/src/optimisation.py[m
[1mindex 9a07021..99ca130 100644[m
[1m--- a/src/optimisation.py[m
[1m+++ b/src/optimisation.py[m
[36m@@ -7,20 +7,18 @@[m [mlogging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(mess[m
 logger = logging.getLogger(__name__)[m
 [m
 [m
[31m-def _group_into_price_blocks(df: pd.DataFrame) -> list[list[pd.Timestamp]]:[m
[31m-    """Group consecutive hours into TenneT's real aFRR bidding blocks.[m
[31m-[m
[31m-    Detected directly from the price data (runs of identical Up/Down price)[m
[31m-    rather than a hardcoded cutover date, so it self-adapts: 24-hour blocks[m
[31m-    before ~2025-07, 4-hour blocks after, and anything TenneT changes to in[m
[31m-    future data without needing this code updated.[m
[32m+[m[32mdef _group_into_price_blocks(df: pd.DataFrame, columns: list[str]) -> list[list[pd.Timestamp]]:[m
[32m+[m[32m    """Group consecutive hours into TenneT's real bidding blocks for the[m
[32m+[m[32m    given price column(s), detected directly from the data (runs of[m
[32m+[m[32m    identical price) rather than a hardcoded cutover date. Self-adapts:[m
[32m+[m[32m    24-hour blocks before ~2025-07, 4-hour after, and anything TenneT[m
[32m+[m[32m    changes to in future data without needing this code updated. aFRR and[m
[32m+[m[32m    FCR are checked separately since they're independent products that[m
[32m+[m[32m    needn't share a block boundary.[m
     """[m
     blocks = [[df.index[0]]][m
     for prev_t, t in zip(df.index, df.index[1:]):[m
[31m-        same_block = ([m
[31m-            df.loc[t, "afrr_up"] == df.loc[prev_t, "afrr_up"][m
[31m-            and df.loc[t, "afrr_down"] == df.loc[prev_t, "afrr_down"][m
[31m-        )[m
[32m+[m[32m        same_block = all(df.loc[t, c] == df.loc[prev_t, c] for c in columns)[m
         if same_block:[m
             blocks[-1].append(t)[m
         else:[m
[36m@@ -29,30 +27,35 @@[m [mdef _group_into_price_blocks(df: pd.DataFrame) -> list[list[pd.Timestamp]]:[m
 [m
 [m
 def run_lp_dispatch([m
[31m-    day_ahead: pd.Series, afrr_up: pd.Series, afrr_down: pd.Series, config: dict[m
[32m+[m[32m    day_ahead: pd.Series,[m
[32m+[m[32m    afrr_up: pd.Series,[m
[32m+[m[32m    afrr_down: pd.Series,[m
[32m+[m[32m    fcr: pd.Series,[m
[32m+[m[32m    config: dict,[m
 ) -> pd.DataFrame:[m
     """Full-horizon LP: co-optimises day-ahead arbitrage against asymmetric[m
[31m-    aFRR capacity reservation (PuLP + CBC). Sees the entire price series[m
[31m-    before deciding anything, so this is a perfect-foresight ceiling, not a[m
[31m-    realistic achievable strategy — report it alongside a rolling-horizon[m
[31m-    version, not on its own.[m
[32m+[m[32m    aFRR capacity and symmetric FCR capacity reservation (PuLP + CBC). Sees[m
[32m+[m[32m    the entire price series before deciding anything, so this is a[m
[32m+[m[32m    perfect-foresight ceiling, not a realistic achievable strategy — report[m
[32m+[m[32m    it alongside a rolling-horizon version, not on its own.[m
 [m
[31m-    Same capacity-coupling convention as heuristic.py, for a fair[m
[31m-    comparison: reserving Up capacity competes with discharge headroom,[m
[31m-    reserving Down capacity competes with charge headroom, each capped at[m
[31m-    the battery's power rating.[m
[32m+[m[32m    FCR is symmetric (one price, one MW figure) rather than Up/Down like[m
[32m+[m[32m    aFRR, because it responds automatically to frequency deviations in[m
[32m+[m[32m    either direction — so a reserved FCR MW competes for headroom on BOTH[m
[32m+[m[32m    the charge and discharge side simultaneously, unlike aFRR's Up/Down[m
[32m+[m[32m    which each only compete on one side.[m
 [m
     Enforces TenneT's LER (Limited Energy Resource) rule: reserved capacity[m
[31m-    must be backed by enough real energy (Up) or headroom (Down) to sustain[m
[31m-    it for a full 15-minute ISP, not just fit under the power rating. Without[m
[31m-    this, the LP can reserve Up capacity from an empty battery for free.[m
[31m-[m
[31m-    Enforces the real aFRR block-granularity rule: reservation must stay[m
[31m-    constant within each bidding block (24h before ~2025-07, 4h after,[m
[31m-    detected empirically from the price data — see _group_into_price_blocks).[m
[31m-    Without this, the LP gets an hour-by-hour flexibility that didn't[m
[31m-    actually exist for most of the backtest window, inflating pre-2025[m
[31m-    revenue relative to what was really achievable.[m
[32m+[m[32m    (aFRR + FCR together) must be backed by enough real energy (Up/FCR) or[m
[32m+[m[32m    headroom (Down/FCR) to sustain it for a full 15-minute ISP, not just fit[m
[32m+[m[32m    under the power rating. Without this, the LP can reserve capacity from[m
[32m+[m[32m    an empty battery for free.[m
[32m+[m
[32m+[m[32m    Enforces block-granularity separately for aFRR and FCR: each must stay[m
[32m+[m[32m    constant within its own real bidding block (detected empirically from[m
[32m+[m[32m    the price data — see _group_into_price_blocks). Without this, the LP[m
[32m+[m[32m    gets hour-by-hour flexibility that didn't actually exist for most of[m
[32m+[m[32m    the backtest window, inflating revenue relative to what was achievable.[m
     """[m
     battery = config["battery"][m
     power_mw = battery["power_mw"][m
[36m@@ -62,7 +65,7 @@[m [mdef run_lp_dispatch([m
     isp_hours = 0.25  # Dutch Imbalance Settlement Period = 15 minutes[m
 [m
     df = pd.DataFrame([m
[31m-        {"day_ahead": day_ahead, "afrr_up": afrr_up, "afrr_down": afrr_down}[m
[32m+[m[32m        {"day_ahead": day_ahead, "afrr_up": afrr_up, "afrr_down": afrr_down, "fcr": fcr}[m
     ).dropna()[m
     hours = list(df.index)[m
 [m
[36m@@ -72,31 +75,40 @@[m [mdef run_lp_dispatch([m
     discharge = pulp.LpVariable.dicts("discharge", hours, lowBound=0)[m
     afrr_up_mw = pulp.LpVariable.dicts("afrr_up_mw", hours, lowBound=0)[m
     afrr_down_mw = pulp.LpVariable.dicts("afrr_down_mw", hours, lowBound=0)[m
[32m+[m[32m    fcr_mw = pulp.LpVariable.dicts("fcr_mw", hours, lowBound=0)[m
     soc = pulp.LpVariable.dicts("soc", hours, lowBound=0, upBound=energy_mwh)[m
 [m
[31m-    for block in _group_into_price_blocks(df):[m
[32m+[m[32m    for block in _group_into_price_blocks(df, ["afrr_up", "afrr_down"]):[m
         anchor = block[0][m
         for t in block[1:]:[m
             problem += afrr_up_mw[t] == afrr_up_mw[anchor], f"block_up_{t}"[m
             problem += afrr_down_mw[t] == afrr_down_mw[anchor], f"block_down_{t}"[m
 [m
[32m+[m[32m    for block in _group_into_price_blocks(df, ["fcr"]):[m
[32m+[m[32m        anchor = block[0][m
[32m+[m[32m        for t in block[1:]:[m
[32m+[m[32m            problem += fcr_mw[t] == fcr_mw[anchor], f"block_fcr_{t}"[m
[32m+[m
     problem += pulp.lpSum([m
         discharge[t] * df.loc[t, "day_ahead"][m
         - charge[t] * df.loc[t, "day_ahead"][m
         + afrr_up_mw[t] * df.loc[t, "afrr_up"][m
         + afrr_down_mw[t] * df.loc[t, "afrr_down"][m
[32m+[m[32m        + fcr_mw[t] * df.loc[t, "fcr"][m
         - (charge[t] + discharge[t]) * cycle_cost[m
         for t in hours[m
     )[m
 [m
     prev_soc = 0.0[m
     for t in hours:[m
[31m-        problem += discharge[t] + afrr_up_mw[t] <= power_mw, f"up_headroom_{t}"[m
[31m-        problem += charge[t] + afrr_down_mw[t] <= power_mw, f"down_headroom_{t}"[m
[31m-        # LER rule: reservation must be backed by energy (Up) or headroom (Down)[m
[31m-        # actually available at the start of the hour, for a full ISP.[m
[31m-        problem += afrr_up_mw[t] * isp_hours <= prev_soc, f"ler_up_{t}"[m
[31m-        problem += afrr_down_mw[t] * isp_hours <= energy_mwh - prev_soc, f"ler_down_{t}"[m
[32m+[m[32m        problem += discharge[t] + afrr_up_mw[t] + fcr_mw[t] <= power_mw, f"up_headroom_{t}"[m
[32m+[m[32m        problem += charge[t] + afrr_down_mw[t] + fcr_mw[t] <= power_mw, f"down_headroom_{t}"[m
[32m+[m[32m        # LER rule: reservation must be backed by energy (Up/FCR) or headroom[m
[32m+[m[32m        # (Down/FCR) actually available at the start of the hour, for a full ISP.[m
[32m+[m[32m        problem += (afrr_up_mw[t] + fcr_mw[t]) * isp_hours <= prev_soc, f"ler_up_{t}"[m
[32m+[m[32m        problem += ([m
[32m+[m[32m            (afrr_down_mw[t] + fcr_mw[t]) * isp_hours <= energy_mwh - prev_soc[m
[32m+[m[32m        ), f"ler_down_{t}"[m
         problem += soc[t] == prev_soc + charge[t] * efficiency - discharge[t], f"soc_{t}"[m
         prev_soc = soc[t][m
 [m
[36m@@ -106,9 +118,10 @@[m [mdef run_lp_dispatch([m
     records = [][m
     for t in hours:[m
         c, d = charge[t].value(), discharge[t].value()[m
[31m-        au, ad = afrr_up_mw[t].value(), afrr_down_mw[t].value()[m
[32m+[m[32m        au, ad, f = afrr_up_mw[t].value(), afrr_down_mw[t].value(), fcr_mw[t].value()[m
         day_ahead_revenue = d * df.loc[t, "day_ahead"] - c * df.loc[t, "day_ahead"][m
         afrr_revenue = au * df.loc[t, "afrr_up"] + ad * df.loc[t, "afrr_down"][m
[32m+[m[32m        fcr_revenue = f * df.loc[t, "fcr"][m
         cycling_cost = (c + d) * cycle_cost[m
         records.append([m
             {[m
[36m@@ -117,11 +130,13 @@[m [mdef run_lp_dispatch([m
                 "discharge_mwh": d,[m
                 "afrr_up_mw": au,[m
                 "afrr_down_mw": ad,[m
[32m+[m[32m                "fcr_mw": f,[m
                 "soc_mwh": soc[t].value(),[m
                 "day_ahead_revenue": day_ahead_revenue,[m
                 "afrr_revenue": afrr_revenue,[m
[32m+[m[32m                "fcr_revenue": fcr_revenue,[m
                 "cycling_cost": cycling_cost,[m
[31m-                "net_revenue": day_ahead_revenue + afrr_revenue - cycling_cost,[m
[32m+[m[32m                "net_revenue": day_ahead_revenue + afrr_revenue + fcr_revenue - cycling_cost,[m
             }[m
         )[m
 [m
[36m@@ -138,6 +153,7 @@[m [mif __name__ == "__main__":[m
     from src.data_pipeline import ([m
         fetch_afrr_capacity_prices,[m
         fetch_day_ahead_prices,[m
[32m+[m[32m        fetch_fcr_capacity_prices,[m
         load_config,[m
     )[m
 [m
[36m@@ -146,9 +162,14 @@[m [mif __name__ == "__main__":[m
 [m
     day_ahead = fetch_day_ahead_prices(start, end)[m
     afrr = fetch_afrr_capacity_prices(start, end)[m
[32m+[m[32m    fcr = fetch_fcr_capacity_prices(start, end)[m
 [m
[31m-    result = run_lp_dispatch(day_ahead, afrr["Up"], afrr["Down"], config)[m
[32m+[m[32m    result = run_lp_dispatch(day_ahead, afrr["Up"], afrr["Down"], fcr, config)[m
     print(result.head(10))[m
[31m-    print(result[["day_ahead_revenue", "afrr_revenue", "cycling_cost", "net_revenue"]].sum())[m
[32m+[m[32m    print([m
[32m+[m[32m        result[[m
[32m+[m[32m            ["day_ahead_revenue", "afrr_revenue", "fcr_revenue", "cycling_cost", "net_revenue"][m
[32m+[m[32m        ].sum()[m
[32m+[m[32m    )[m
     both_active = ((result["charge_mwh"] > 1e-6) & (result["discharge_mwh"] > 1e-6)).sum()[m
     print(f"Hours with simultaneous charge AND discharge: {both_active}")[m
